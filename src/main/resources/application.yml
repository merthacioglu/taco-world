spring:
  application:
    name: taco-world
  datasource:
    generate-unique-name: false
    name: taco-world
    url: jdbc:h2:mem:testdb
    driver-class-name: org.h2.Driver
    username: sa
    password: password
  h2:
    console:
      enabled: true